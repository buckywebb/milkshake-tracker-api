<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cookout Milkshake App</title>
</head>
<body>
    <h1>Cookout Milkshake Tracker</h1>

    <form action="/flavors" method="POST">
        <input type="text" placeholder="flavor" name="flavor">
        <input type="text" placeholder="rating" name="rating">
        <button type="submit">Submit</button>
    </form>

    <h2>Flavors</h2>

    <ul class="flavors">
        <!-- Loop through quotes -->
        <% for(var i = 0; i < flavors.length; i++) {%>
        <li class="flavor">
          <!-- Output name from the iterated quote object -->
          <span><%= flavors[i].flavor %></span>:
          <!-- Output quote from the iterated quote object -->
          <span><%= flavors[i].rating %></span>
        </li>
        <% } %>
      </ul>

      <div>
        <h2>You're Wal-NUTS!</h2>
        <p>Replace first Walnut rating with the truth!</p>
        <button id="update-button">Click if you went wal-NUTS!</button>
      </div>

      <div>
        <h2>Remove a Walnut Shake</h2>
        <p>Delete a Walnut Shake(if it exists)</p>
        <button id="delete-button">Delete a Walnut Shake</button>
      </div>
      <div id="message"></div>
    
    <script src="/main.js"></script>
</body>
</html>